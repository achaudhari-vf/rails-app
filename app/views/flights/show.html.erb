<div class="card">
  <div class="card-body">
    This flight info according to id 
  </div>
</div>
<% flash.each do |name, msg| %> 
  <%= msg %> 
<% end %>
<%= button_to 'Show all Flights', flights_path ,method: :get %>
<table class="table table-dark table-striped-columns">
    <thead>
        <tr>
            <th>Source</th>
            <th>Destination</th>
            <th>Price</th>
        </tr>
    </thead>
    <tbody>
        
            <tr>
                <td><%= @flights.source %></td>
                <td><%= @flights.destination %></td>
                <td><%= @flights.price %></td>
            </tr>
       
    </tbody>
</table>
